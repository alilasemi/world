compile_commands.json
*.swo
*.swp
.cache/
build/
external/
node_modules/
package-lock.json
stability_results.dat
stability_plot.gp
stability_plot.png
history.dat
trajectory_plot.gp
trajectory_plot.png

# Python virtualenv for the surrogate tooling (recreate with surrogate/requirements.txt)
surrogate/.venv/
__pycache__/

# Generated dataset artifacts (large; regenerate with build/bin/dataset).
# The base config dataset/blob_throw.yaml IS tracked.
dataset/*.bin
dataset/*.csv

# Fitted surrogate artifacts (large; regenerate with fit_dynamics.py --save)
surrogate/*.joblib
decoded/
dataset_dynamics/
dataset_dyn2/
decoded_uniform/

# Generated by build/bin/chaos (ensemble.yaml IS tracked)
chaos/*.csv
chaos/*.bin

# Figures regenerated by the surrogate scripts; the tracked copies live in assets/screenshots/
surrogate/*.png
